{% extends 'loggedIn.twig' %}

{% block body %}
    <table>
        <thead>
            <tr>
                <th>Set</th>
                <th>Description</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            {% for set in sets %}
                <tr>
                    <td>{{ set.title }}</td>
                    <td>
                        {% if activationChecker.setActiveForRepository(set, gitRepository) %}
                            <a href="{{ path('deactivate_set_github', {'githubRepositoryId': gitRepository.githubRepositoryId, 'rectorSetName': set.name}) }}">
                                Deactivate
                            </a>
                        {% else %}
                            <a href="{{ path('activate_set_github', {'githubRepositoryId': gitRepository.githubRepositoryId, 'rectorSetName': set.name}) }}">
                                Activate
                            </a>
                        {% endif %}
                    </td>
                </tr>
            {% endfor %}
        </tbody>
    </table>
{% endblock %}
