{% extends 'loggedIn.twig' %}

{% block body %}
    <table>
        <thead>
            <tr>
                <th>Set</th>
                <th>Description</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            {% for set in sets %}
                <tr>
                    <td>{{ set.title }}</td>
                    <td>
                        <a href="{{ path('install_set_github', {'githubRepositoryId': githubRepositoryId, 'rectorSetName': set.name}) }}">
                            Activate
                        </a>


                        {# TODO: in_array installed sets #}
                        {#
                        {% if activationChecker.setActiveForRepository(set, gitRepository) %}
                            <a href="{{ path('uninstall_set_github', {'githubRepositoryId': gitRepository.githubRepositoryId, 'rectorSetName': set.name}) }}">
                                Deactivate
                            </a>
                        {% else %}
                            <a href="{{ path('install_set_github', {'githubRepositoryId': gitRepository.githubRepositoryId, 'rectorSetName': set.name}) }}">
                                Activate
                            </a>
                        {% endif %}
                        #}
                    </td>
                </tr>
            {% endfor %}
        </tbody>
    </table>
{% endblock %}
